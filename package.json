{
  "name": "job-search-kit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "bin": {
    "jobs": "./src/cli/jobs.mjs"
  },
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "configure": "node scripts/configure.mjs",
    "install:agent": "node scripts/install-agent.mjs",
    "jobs": "node src/cli/jobs.mjs",
    "dashboard": "node src/cli/jobs.mjs dashboard",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs"
  },
  "dependencies": {
    "js-yaml": "^4.1.1"
  },
  "description": "An interview-driven job-search workspace for coding agents"
}
